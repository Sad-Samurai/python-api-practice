# WordPress API Practice

A small Python script I created while learning how to interact
with REST APIs using the `requests` library.

## What it does

- Sends a GET request to a WordPress REST API endpoint
- Parses the JSON response
- Extracts post IDs
- Prints post titles

## Requirements

```bash
pip install requests
